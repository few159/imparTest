import styled from 'styled-components'

export const SearchSectionElement = styled.section`
    div{
        display: flex;
        justify-content: center;
        overflow: hidden;
        margin-top: -4rem;
        position: relative;
        z-index: -1;

        img{
            min-width: fit-content;
            width: 100%;
        }
    }
`